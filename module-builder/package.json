{
  "name": "@nexus-app/nexus-module-builder",
  "version": "1.0.0",
  "description": "Reusable TypeScript settings and components for Nexus",

  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "node builder.js && tsc",
    "publish": "npm run build && cd ./dist && npm publish --access public"
  },
  "author": "aarontburn",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.13.14",
    "typescript": "^5.0.0"
  }
}
