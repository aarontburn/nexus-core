{
  "name": "@nexus/nexus-client",
  "version": "1.0.0",
  "description": "Micro-app platform.",
  "scripts": {
    "build": "node builder.js && tsc && node copyFiles.js",
    "watch": "tsc -w",
    "start": "npm run build && electron ./dist/main.js --dev",
    "package": "npm run build && npx electron-builder build",
    "update-nexus": "npm update nexus-module-builder",
    "core-start": "npm run build && electron ./dist/main.js --dev --in-core"
  },
  "main": "./main.js",
  "author": "aarontburn",
  "devDependencies": {
    "@types/yauzl-promise": "^4.0.1",
    "electron": "^28.0.0",
    "electron-builder": "^24.13.3",
    "typescript": "^4.8.3"
  },
  "dependencies": {
    "@nexus/nexus-module-builder": "https://gitpkg.vercel.app/aarontburn/nexus-core/module-builder/dist?main",
    "typescript": "^4.8.3",
    "yauzl-promise": "^4.0.0"
  },
  "productName": "Nexus",
  "build": {
    "appId": "com.aarontburn.Nexus",
    "asar": true,
    "files": [
      "./dist/**/*"
    ],
    "directories": {
      "output": "package"
    }
  }
}
